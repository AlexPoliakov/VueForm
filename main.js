const app = new Vue({
    el: '#app',
    components: {
        form_registration: {
            template: '#reg__form'
        },

        list: {
            template: '#user__list'
        }
    }
});
